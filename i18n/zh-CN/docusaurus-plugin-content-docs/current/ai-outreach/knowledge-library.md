---
id: knowledge-library
title: 知识库
sidebar_label: 知识库
description: 构建您的知识库，上传文档供 AI 使用以生成具有上下文相关性的内容。
---

# 知识库

知识库是 aiFetchly 的智能文档管理系统。上传您的文档（PDF、Word、HTML 等格式），构建为 AI 内容生成提供支撑的知识库，确保您的外联内容精准且个性化。

:::info 需要订阅

知识库（包括免费的本地嵌入模型）需要 aiFetchly 订阅。如果您的账户未启用 AI，页面会显示**需要订阅**提示。

:::

## 什么是 RAG？

**RAG**（检索增强生成）是一项技术，其工作流程如下：

1. **摄取**您的文档，并将其拆分为较小的片段
2. **创建向量嵌入**，理解内容的语义含义
3. 在生成内容时**检索相关信息**
4. 利用您的专属知识**增强 AI 回复**

:::info RAG 为何重要

传统 AI 系统生成的内容较为泛化。借助 RAG，aiFetchly 的 AI 能够引用您的文档，创建个性化、具备上下文感知能力的邮件和营销内容。

:::

## 支持的文件类型

| 格式 | 扩展名 | 适用场景 |
|--------|------------|----------|
| **PDF** | `.pdf` | 宣传册、白皮书、技术文档 |
| **Microsoft Word** | `.doc`, `.docx` | 提案、合同、产品资料 |
| **文本** | `.txt` | 纯文本文件、笔记 |
| **Markdown** | `.md` | 技术文档、README 文件 |
| **HTML** | `.html`, `.htm` | 网页内容、文章 |

## 上传文档

### 第 1 步：进入知识库

1. 点击左侧导航菜单中的 **Knowledge**
2. 您将看到知识库界面

### 第 2 步：上传文档

**方式一：拖放上传**

1. 从电脑中拖动文件
2. 将文件拖放到上传区域
3. 界面会显示文件正在添加的视觉反馈

**方式二：文件浏览器**

1. 点击 **Upload** 按钮（或上传区域）
2. 在文件浏览器中找到您的文件
3. 选择一个或多个文档
4. 点击 **Open** 开始上传

### 第 3 步：处理

上传后，文档将自动进行处理：

1. **保存**：文件保存到数据库
2. **分块**：文档被拆分为较小的片段
3. **嵌入**：创建向量嵌入用于语义搜索
4. **状态更新**：处理状态从 **待处理** → **处理中** → **已完成** 变化

:::tip 处理时间

处理时间取决于文件大小：
- 小文件（< 1MB）：10-30 秒
- 中等文件（1-5MB）：30-60 秒
- 大文件（5-10MB）：1-3 分钟

:::

## 管理文档

### 文档列表视图

知识库会展示所有文档，包含以下信息：

| 列 | 说明 |
|--------|-------------|
| **Name** | 文档文件名 |
| **Title** | 文档标题（可编辑） |
| **Status** | 处理状态（待处理/处理中/已完成/错误） |
| **Type** | 文件类型（PDF、DOCX 等） |
| **Size** | 文件大小 |
| **Upload Date** | 文档上传日期 |
| **Actions** | 查看、下载、删除、重新嵌入 |

### 文档操作

| 操作 | 说明 |
|--------|-------------|
| **View** | 打开文档查看内容 |
| **Download** | 将原始文件下载到电脑 |
| **Delete** | 从知识库中移除文档 |
| **Re-embed** | 使用新的嵌入模型重新处理文档 |
| **View Logs** | 查看失败文档的错误详情 |

### 搜索与筛选

- **按名称搜索**：根据文件名筛选文档
- **按状态筛选**：仅显示已完成、处理中或失败的文档
- **按类型筛选**：仅显示特定文件类型

### 批量操作

- **多选**：勾选文档旁的复选框
- **批量删除**：一次移除多个文档
- **清除选择**：取消选中所有文档

## 了解处理状态

| 状态 | 颜色 | 含义 | 操作 |
|--------|-------|---------|--------|
| **待处理** | 灰色 | 已排队等待处理 | 等待自动处理 |
| **处理中** | 蓝色 | 正在进行嵌入处理 | 等待完成 |
| **已完成** | 绿色 | 可用于 AI 内容生成 | 文档已激活 |
| **错误** | 红色 | 处理失败 | 查看日志，尝试重新嵌入 |

## 重新嵌入文档

如果您更换了嵌入模型或需要重新处理文档：

1. 在列表中找到该文档
2. 点击 **Re-embed** 按钮
3. 文档状态变为 **处理中**
4. 使用当前模型创建新的嵌入
5. 完成后状态更新为 **已完成**

**重新嵌入的适用场景：**
- 在设置中更换了嵌入模型
- 上次嵌入部分失败
- 希望使用更新后的分块参数

关于如何选择或更换模型，请参阅下方的[嵌入模型设置](#嵌入模型设置)。

## 嵌入模型设置

嵌入将您的文本转换为 AI 进行语义搜索时所使用的向量。您可以在**设置**中指定 aiFetchly 使用的嵌入模型。

### 打开嵌入设置

1. 打开**知识库**。
2. 点击顶部的**设置**。

对话框会显示一个**嵌入模型**下拉列表，列出所有可用模型、您的**当前模型**，以及一个**更新模型**按钮。

### 本地模型与远程模型

下拉列表中包含两类模型：

| 类型 | 示例 | 说明 |
|------|---------|-------|
| **本地（免费）** | `Xenova/all-MiniLM-L6-v2 (free)` | 完全在您的设备上运行（384 维）。免费、私密，缓存后可离线使用。 |
| **远程** | 服务器提供的模型 | 在 aiFetchly 的 AI 服务器上生成。根据您的套餐免费或付费。 |

免费模型——无论是本地还是远程——在下拉列表中都会显示绿色的**免费**标识。本地模型始终会列出，即使远程模型列表无法加载，因此总有一个可用的选项。

:::tip 本地模型 = 隐私 + 免费

本地模型通过 Transformers.js 在您自己的 CPU 上运行。选中该模型时，您的文档文本**绝不会发送到远程嵌入端点**——嵌入完全在您的机器上生成。选择它可实现隐私保护、零成本和离线使用。

:::

:::note 首次使用需下载模型

首次使用本地模型时，aiFetchly 会（从 Hugging Face）下载模型权重并缓存到磁盘。因此首次索引会比较慢；后续运行会复用已缓存的模型，速度较快。

:::

选择一个模型并点击**更新模型**。新模型将应用于**后续**的索引和重新嵌入。已有文档会保留其最初嵌入时所用的模型，直到您对其重新嵌入。

### 索引过程中的自动回退

如果您使用的是远程模型，且嵌入在重试后仍然失败（例如网络或服务器错误），aiFetchly 会自动：

1. 丢弃该文档的所有部分向量，以免混淆不同的嵌入空间。
2. 使用**本地免费模型**对整个文档重新嵌入。
3. 在文档上记录所使用的本地模型。

您会看到：*"远程嵌入失败。AiFetchly 已改用本地免费的嵌入模型。"*

如果本地模型也失败，文档将被标记为**错误**，并显示：*"远程重试和本地回退后，嵌入生成仍然失败。请查看文档错误日志以了解详情。"*

:::warning 配额错误不会回退

如果远程失败是由于账单或配额上限引起的，aiFetchly **不会**静默回退——它会显示*"已达到远程嵌入配额或账单上限"*，以便您为套餐充值。切换到本地模型（或在解决配额问题后重新嵌入）即可继续。

:::

### 搜索混合模型的知识库

如果部分文档使用远程模型嵌入，而其他文档使用本地模型嵌入，则每份文档将使用其嵌入时所用的模型进行搜索——两种嵌入空间不可互换。如果搜索期间远程端点不可用，则该次搜索会跳过以远程模型索引的文档，仅以本地模型索引的文档返回结果。

## 故障排除

### 文档状态："错误"

**可能原因：**
- 文件已损坏
- 不支持的文件格式
- 文件过大
- 编码问题

**解决方案：**
1. **查看日志**以了解具体错误
2. **尝试重新嵌入**该文档
3. **重新上传**原始文件
4. **转换文件**为其他格式（例如 DOC → PDF）

### 处理速度慢

**可能原因：**
- 文件较大
- 系统负载高
- 网络延迟（远程嵌入场景）
- 首次使用本地嵌入模型（需一次性下载模型权重）

**解决方案：**
1. 等待处理完成
2. 将大型文档拆分为较小的文件
3. 关闭其他应用程序以释放资源
4. 如果您刚刚切换到本地模型，首次运行会下载并缓存模型——后续运行会很快

### 文档未被 AI 内容引用

**可能原因：**
- 文档尚未完全处理
- 文档内容与查询不相关
- RAG 上下文未启用

**解决方案：**
1. 确认文档状态为**已完成**
2. 确保在 AI Chat/Email Writer 中已启用 RAG 上下文
3. 尝试搜索更具体的内容
4. 上传更多相关文档

## 最佳实践

### 1. 文档选择

**建议上传以下类型的文档：**
- 详细描述您的产品或服务的文档
- 阐述您的价值主张的文档
- 包含案例研究或成功案例的文档
- 包含行业专业术语的文档
- 提供竞争优势信息的文档

**避免上传：**
- 泛化或过时的信息
- 不相关的内容
- 过大的文件（> 10MB）
- 格式混乱的文档

### 2. 文档组织

**命名规范：**
- 使用描述性名称：`Product_Brochure_2024.pdf`
- 包含版本号：`Pricing_Guide_v2.docx`
- 添加日期：`Case_Study_January_2024.pdf`

**分类管理：**
- 将相关文档归类在一起
- 使用一致的命名模式
- 为文档添加标签以便筛选

### 3. 内容质量

**为获得最佳效果：**
- 使用格式良好的文档
- 包含结构化的标题
- 提供具体细节和示例
- 保持信息更新
- 使用专业语言

### 4. 定期维护

**保持知识库健康：**
- **定期检查**：移除过时的文档
- **更新内容**：信息变更时重新上传
- **监控状态**：检查是否有失败的嵌入
- **优化大小**：尽可能拆分大型文档

## 与 AI 功能的集成

知识库可与以下功能集成：

### AI 邮件撰写器

创建 AI 生成的邮件时：

1. 在邮件撰写器中**启用 RAG 上下文**
2. AI 在知识库中搜索相关信息
3. 检索到的内容用于个性化邮件
4. 邮件包含准确且具备上下文感知的信息

**示例：**
- 您上传了一份产品目录 PDF
- AI 生成引用特定产品的邮件
- 每封邮件都提及与收件人相关的产品

### AI 营销助手

与 AI 助手对话时：

1. **切换 RAG 上下文**（📖 图标）
2. 提问关于您的业务、产品或服务的问题
3. AI 在知识库中搜索答案
4. 回复基于您的文档内容

**示例问题：**
- "我们的核心产品功能有哪些？"
- "我们的定价与竞争对手相比如何？"
- "我们的退款政策是什么？"
- "为产品 X 生成一封营销邮件"

## 示例使用场景

### 场景一：产品营销

**建议上传的文档：**
- 产品宣传册
- 功能规格说明
- 定价指南
- 竞品对比表
- 案例研究

**效果：** AI 生成详细、准确的产品邮件。

### 场景二：服务型企业

**建议上传的文档：**
- 服务描述
- 流程文档
- 客户评价
- 作品集样本
- 定价方案

**效果：** AI 创建包含具体细节的服务导向外联内容。

### 场景三：代理机构外联

**建议上传的文档：**
- 代理机构能力介绍
- 作品集
- 案例研究
- 团队简介
- 服务套餐

**效果：** AI 为每个潜在客户个性化定制代理机构推介。

### 场景四：SaaS 企业

**建议上传的文档：**
- 功能文档
- API 指南
- 定价层级
- 入门指南
- 网络研讨会文字记录

**效果：** AI 生成专业且易于理解的外联内容。

## 技术细节

### RAG 的工作原理

1. **文档摄取**：
   - 文件上传并保存到数据库
   - 记录元数据（名称、类型、大小、日期）

2. **文本提取**：
   - 从不同文件格式中提取文本
   - 尽可能保留格式

3. **分块**：
   - 文档被拆分为较小的片段（chunks）
   - 典型分块大小：500-1000 个字符
   - 分块之间保留重叠以维持上下文

4. **嵌入创建**：
   - 每个分块被转换为向量嵌入
   - 嵌入捕获语义含义
   - 存储在向量数据库中以实现快速检索

5. **语义搜索**：
   - 生成内容时，AI 搜索相关分块
   - 通过相似度匹配找到最相关的内容
   - 检索到的分块作为上下文纳入

6. **内容生成**：
   - AI 使用检索到的上下文和提示词
   - 生成个性化、准确的内容
   - 引用您的专属知识

### 存储与性能

- **存储**：文档存储在本地 SQLite 数据库中
- **向量数据库**：针对快速相似度搜索进行了优化
- **性能**：毫秒级检索相关内容
- **可扩展性**：高效处理数千份文档

## 安全与隐私

### 数据存储

- **本地存储**：所有文档存储在您的本地机器上
- **无云端上传**：原始文件保留在您的电脑上
- **加密**：数据库可进行加密以增强安全性

### 隐私说明

- **您的知识**：只有您可以访问您的文档
- **AI 处理**：使用本地嵌入模型时，文本不会离开您的设备；使用远程模型时，嵌入在 aiFetchly 的服务器上生成
- **不用于训练**：您的文档不会用于训练公共 AI 模型

:::tip 机密信息

知识库非常适合存储以下内容：
- 内部产品文档
- 机密定价信息
- 专有业务流程
- 特定客户信息

:::

## 下一步

知识库构建完成后，您可以继续：

- [创建 AI 生成的邮件营销活动](./ai-email-writer)
- [使用 AI 营销助手](./ai-marketing-assistant)
- [设置批量邮件发送](../lead-generation/batch-email-sending)

---

**准备好构建您的知识库了吗？** 从上传您的产品文档、定价指南和营销资料开始，为个性化的 AI 外联提供支撑。
